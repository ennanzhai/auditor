sia directory is for structural independence auditing

'auditor/src/sia/test/hard/test-1' can parse different hardware xml, and
create a file that could be directly read by our auditor program.

Run test.py to output the desired results. 
